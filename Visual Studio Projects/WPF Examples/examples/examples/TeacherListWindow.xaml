<Window x:Class="examples.TeacherListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:examples"
        mc:Ignorable="d"
        Title="TeacherListWindow" Height="450" Width="800" FontSize="14" Background="#04c582">
    <StackPanel>
        <DataGrid Name="TeachersGrid" Height="415" AutoGenerateColumns="False" Margin="6,10,6,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="TeacherID" Binding="{Binding Path=TeacherID}"></DataGridTextColumn>
                <DataGridTextColumn Header="TeacherFirstname" Binding="{Binding Path=TeacherFirstName}"></DataGridTextColumn>
                <DataGridTextColumn Header="TeacherLastname" Binding="{Binding Path=TeacherLastName}"></DataGridTextColumn>
                <DataGridTextColumn Header="TeacherAge" Binding="{Binding Path=TeacherAge}"></DataGridTextColumn>
                <DataGridTextColumn Header="TeacherMail" Binding="{Binding Path=TeacherMail}"></DataGridTextColumn>
                <DataGridTextColumn Header="SubjectID" Binding="{Binding Path=SubjectName}"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
    </StackPanel>
</Window>
